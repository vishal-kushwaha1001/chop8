// Booking routes are registered via chefRoutes.js (same /api prefix)
// This file is intentionally minimal — all booking endpoints live in chefRoutes.js
// to match the original Java @RequestMapping("/api") on ChefController.
module.exports = require('express').Router();
